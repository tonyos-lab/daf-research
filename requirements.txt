# DAF — pinned to the version this research branch tests against
# Each branch pins a specific version. Do not change this without
# creating a new branch.
daf @ git+https://github.com/tonyos-lab/daf@v0.1.1

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-timeout>=2.3.0
hypothesis>=6.100.0

# Experiment tooling
pandas>=2.0.0
matplotlib>=3.8.0

# Code quality
ruff>=0.4.0
